import XCTest
@testable import SwiftUIRefresh

final class SwiftUIRefreshTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SwiftUIRefresh().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
